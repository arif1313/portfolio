

const Title = ({title}) => {
    return (
        <div>
            <h1 className="text-[#28E98C] font-semibold text-3xl text-center p-2 ">{title}</h1>
        </div>
    );
};

export default Title;